# Copy to .env and fill in. Do NOT commit .env.

# Optional: an n8n (or any) webhook URL to receive waitlist leads.
# If unset, submissions are logged to the container console instead.
WAITLIST_WEBHOOK_URL=
